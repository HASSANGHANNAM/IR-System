# IR-System
Built a custom search engine to search and retrieve documents from a dataset. Information retrieval principles were used to design and implement the engine, enabling it to handle user queries and return relevant results in natural language text.
